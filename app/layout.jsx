
export const metadata = {
  title: "alpha",
  description: "auto parts multi-search",
};

export default function RootLayout({ children }) {
  return (
    <html lang="ja">
      <body>{children}</body>
    </html>
  );
}
